"""ResearchOS backend package.

The backend is intentionally small at this stage. Future modules should be
added as explicit packages under ``app`` rather than mixed directly into the
application entrypoint.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
